#ifndef CPP_SPEEDTEST
#define CPP_SPEEDTEST
#include <cstdint>
#include <chrono>
#include <iostream>
namespace speedtest
{
    void sort(uint32_t arr[], int n);
    void swap(uint32_t &a, uint32_t &b);
    void rand_n(uint32_t numbers[], int size);
}
#endif
